
//my answer
function check(a, x) {
    return (a.includes(x))
  }

  const check = (a,x) => a.includes(x);

  function check(a, x) {
    // your code here
  }


  parseInt(string)
  parseInt(string, radix)