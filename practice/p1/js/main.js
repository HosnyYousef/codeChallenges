function sumOfDifferences(arr) {
    let sum = arr.length > 1 ? (Math.max(...arr) - Math.min(...arr) : 0)
}

//Mistakes:
//need to put return
//no need for parentheses


function sumOfDifferences(arr) {
    return arr.length > 1 ? Math.max(...arr) - Math.min (...arr) : 0;
}


function doubleInteger(i) {
    return i * 2;
  }


let doubleInteger = i => i * 2

function sumOfDifferences (robot) {
    return robot.length > 1 ? Math.max(...arr) - Math.min(...arr): 0;
}

const sumOfDifferences = robot => robot.lenth > 1 ? Math.max(...arr) - Math.min (...arr): 0


function even_or_odd(number) {
    if (number % 2 == 0){
        return ('Even')
    } else if (number % 2 !== 0){
        return ('Odd')
    }
}



let even_or_odd = number => number % 2 == 0 ? 'even' : 'odd'



let even_or_odd  = number => (number%2) ? 'even' : 'odd'




function even_or_odd(number) {
    if (number%2 === 0){
        return ('even')
    }else{
        return ('odd')
    }
}

function even_or_odd(number) {
    return (number%2 === 0? 'even' : 'odd')
}

function sumOfDifferences(arr) {
    return arr.length > 1 ? Math.max(...arr) - Math.min(...arr) : 0;
}

let sumOfDifferences = arr => arr.length > 1 ? Math.max(...arr) - Math.min(...arr) : 0



function even_or_odd(number) {
    return number%2 ? 'even' : 'odd'
}

function even_or_odd(number) {
    return number%2 == 0 ? 'even' : 'odd'
}

let even_or_odd = number => number%2 ? 'even' : 'odd'

function even_or_odd(number) {
    if (number%2 == 0){
        return('even')
    }else{
        return('odd')
    }
}

class Kata {
    static getVolumeOfCuboid(length, width, height) {
      return length * width * height
    }
  }


  class Kata {
    static getVolumeOfCuboid(length, width, height) {
      let getVolumeOfCuboid = length * width * height
    }
  }




  
