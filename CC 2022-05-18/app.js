function deleteNth(arr,n){
    const viewings = [1,2,3,1,2,1,2,3];
    for (let index = 0; index < array.length; index++) {
        const element = viewings[index];
        if (ele > 2) break;
    }
  }